import 'package:flutter/material.dart';

class MyBilletsPage extends StatelessWidget {
  const MyBilletsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}